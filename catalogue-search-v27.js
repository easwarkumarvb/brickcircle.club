/* BrickCircle V2.7 — scalable Rebrickable catalogue search. */
(()=>{'use strict';
const PAGE=36, URL='https://nsxtromjdpdscknadxez.supabase.co', KEY='sb_publishable_JJhVbgjGblHrnKuPOsJkxQ_zRoQNlIL';
const catdb=window.supabase.createClient(URL,KEY);let offset=0,busy=false,lastKey='',total=null,mounting=false;
const $=s=>document.querySelector(s),esc=x=>String(x??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot',"'":'&#39;'}[c]));
const money=x=>x==null||Number(x)<=0?'Not yet available':'$'+Number(x).toLocaleString();
const emoji=t=>({Technic:'🏎️',Icons:'🏛️',Ideas:'🚀','Star Wars':'🤖'})[t]||'🧱';
function card(s){return `<div class="card"><div class="icon">${emoji(s.theme)}</div><span class="pill">${esc(s.theme||'LEGO')}</span><h3>${esc(s.name)}</h3><div class="muted" style="font-size:13px">Set ${esc(s.set_number)} · ${s.year||''} · ${(s.piece_count||0).toLocaleString()} pieces</div><p>Market value <b>${money(s.estimated_value)}</b></p><div class="actions"><button class="primary" onclick="addCollection('${esc(s.set_number)}')">Add to collection</button><button onclick="addWish('${esc(s.set_number)}')">Wishlist</button></div></div>`}
function clean(v){return String(v||'').trim().replace(/[,%()]/g,' ')}
async function query(reset=true){if(busy)return;busy=true;const q=clean($('#bcCatQ')?.value),theme=$('#bcCatTheme')?.value||'',year=$('#bcCatYear')?.value||'',key=[q,theme,year].join('|');if(reset||key!==lastKey){offset=0;lastKey=key;total=null}let req=catdb.from('lego_sets').select('set_number,name,year,piece_count,theme,estimated_value',{count:offset===0?'exact':undefined}).eq('catalog_active',true);if(theme)req=req.eq('theme',theme);if(year)req=req.eq('year',Number(year));if(q){const p=`%${q}%`;req=req.or(`set_number.ilike.${p},name.ilike.${p},theme.ilike.${p}`)}req=req.order('year',{ascending:false}).order('set_number',{ascending:true}).range(offset,offset+PAGE-1);const {data,error,count}=await req;busy=false;const list=$('#bcCatList'),status=$('#bcCatStatus'),more=$('#bcCatMore');if(!list)return;if(error){status.textContent='Catalogue search temporarily unavailable.';console.error(error);return}if(offset===0){list.innerHTML='';if(count!=null)total=count}list.insertAdjacentHTML('beforeend',(data||[]).map(card).join('')||(offset===0?'<div class="notice">No matching LEGO sets found.</div>':''));offset+=(data||[]).length;status.textContent=`Showing ${Math.min(offset,total??offset).toLocaleString()}${total!=null?' of '+total.toLocaleString():''} matching sets`;more.style.display=(data||[]).length===PAGE&&(total==null||offset<total)?'inline-block':'none'}
function catalogueMarkup(){const preferred=['Technic','Icons','Ideas','Star Wars','Creator Expert','Architecture','Speed Champions','City','Castle','Space','Pirates','Harry Potter','Marvel Super Heroes','DC Comics Super Heroes','Ninjago'];return `<div class="card" id="bcScalableCatalogue"><h2>LEGO catalogue</h2><p class="muted">Search BrickCircle's comprehensive Rebrickable-powered catalogue. Results load in small batches for speed.</p><div class="toolbar"><input id="bcCatQ" placeholder="Search set number, name or theme" autocomplete="off"><select id="bcCatTheme"><option value="">All themes</option>${preferred.map(t=>`<option>${esc(t)}</option>`).join('')}</select><select id="bcCatYear"><option value="">Any year</option>${Array.from({length:new Date().getFullYear()-1948},(_,i)=>new Date().getFullYear()-i).map(y=>`<option>${y}</option>`).join('')}</select></div><div id="bcCatStatus" class="muted" style="margin:8px 0 15px">Loading catalogue…</div><div id="bcCatList" class="sets"></div><div style="text-align:center;margin:20px"><button id="bcCatMore" class="primary" style="display:none">Load more</button></div></div>`}
function mount(){if(mounting||(location.hash||'#home').slice(1)!=='catalogue'||$('#bcCatList'))return;const app=$('#app');if(!app)return;mounting=true;const wrap=app.querySelector('.wrap');if(wrap){const tabs=wrap.querySelector('.tabs');const tabsHtml=tabs?tabs.outerHTML:'';wrap.innerHTML=tabsHtml+catalogueMarkup()+`<div class="footer">BrickCircle · Rebrickable-powered LEGO catalogue</div>`}else{app.innerHTML=`<div class="wrap">${catalogueMarkup()}<div class="footer">BrickCircle · Rebrickable-powered LEGO catalogue</div></div>`}let timer;$('#bcCatQ').addEventListener('input',()=>{clearTimeout(timer);timer=setTimeout(()=>query(true),250)});$('#bcCatTheme').addEventListener('change',()=>query(true));$('#bcCatYear').addEventListener('change',()=>query(true));$('#bcCatMore').addEventListener('click',()=>query(false));mounting=false;query(true)}
function ensureMount(){if((location.hash||'#home').slice(1)==='catalogue'&&!$('#bcCatList'))mount()}
function hook(){const oldNav=window.bcNav;if(oldNav&&!oldNav.__cat27){const n=p=>{oldNav(p);if(p==='catalogue')setTimeout(ensureMount,0)};n.__cat27=true;window.bcNav=n}window.addEventListener('hashchange',()=>setTimeout(ensureMount,0));const app=$('#app');if(app){new MutationObserver(()=>{if(!mounting)ensureMount()}).observe(app,{childList:true,subtree:true})}[0,100,300,700,1500,3000].forEach(ms=>setTimeout(ensureMount,ms))}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',hook);else hook();})();